import { articleCategory } from '@prisma/client';

// eslint-disable-next-line import/prefer-default-export
export function toArticleCategory(_articleCategory: articleCategory) {
  return _articleCategory;
}
